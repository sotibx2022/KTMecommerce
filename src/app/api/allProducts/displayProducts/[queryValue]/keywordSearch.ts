export const keywordSearch=(keyword:string)=>{
    // if keyword is single and simple word.
    // we need to generate synonyms using langchain.
    // we will raturn array of keywors to the main search function?
    // if keyword has spelling error, like if user type mobil instead of mobile how can we solve this issue?
    // if user type 2 words?
    // if user types one paragraph or one full sentences.

}